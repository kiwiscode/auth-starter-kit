import React from "react";

const Settings: React.FC = () => {
  return (
    <div>
      <h1>Settings</h1>
      <p>User settings will be here. (Visible to logged-in users only)</p>
    </div>
  );
};

export default Settings;
